{
  "name": "pet-assistant-master-v1.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:user": "npm --prefix frontend run web -- --port 8934 --host lan --clear",
    "dev:user:native": "node scripts/start-native-dev.js",
    "dev:admin": "npm --prefix admin-web run dev",
    "dev:all": "node scripts/dev-all.js",
    "build:backend": "npm --prefix backend run build",
    "build:admin": "npm --prefix admin-web run build",
    "check:all": "node scripts/check-all.js",
    "health:check": "node scripts/health-check.js"
  }
}

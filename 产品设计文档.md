# 宠物助手产品设计文档

## 1. 文档目标

- 产品名称：宠物助手
- 当前版本：V1.1
- 文档目的：统一当前真实交付状态、完整版推进方向、MVP 后续实施顺序
- 适用对象：产品、前端、后端、测试、答辩演示整理

## 2. 当前三端基线

当前仓库已经稳定为三端结构：

- 用户端：`frontend`
- 管理端：`admin-web`
- 后端：`backend`

默认开发端口：

- 后端：`4317`
- 用户端 Web：`8934`
- 管理端 Web：`5617`

默认运行方式：

```bash
npm run dev:backend
npm run dev:user
npm run dev:admin
```

数据库基线：

- 当前默认继续使用 `sql.js`
- 本地演示不依赖 MySQL
- 生产化数据库迁移不作为当前阶段前置条件

## 3. 产品定位

宠物助手面向宠物主人提供一套围绕“记录、查询、陪伴、内容获取”的综合工具，同时提供一个后台给平台管理员进行用户与内容运营。

产品分两层：

- 用户端：宠物档案、健康记录、护理记录、AI 问答、社区、知识百科、个人设置
- 管理端：管理员登录、概览统计、用户管理、内容管理，后续扩展系统配置与数据分析

## 4. 当前已交付范围

### 4.1 用户端

#### 账号体系

- 注册
- 登录
- 获取个人资料
- 修改个人资料
- 修改密码
- 绑定邮箱

#### 宠物管理

- 宠物列表
- 新增宠物
- 编辑宠物
- 删除宠物

#### 健康管理

- 疫苗记录增删改查
- 驱虫记录增删改查
- 体检记录增删改查
- 所有记录按宠物归属校验当前登录用户

#### 日常护理

- 护理记录列表
- 新增护理记录
- 编辑护理记录
- 删除护理记录
- 后端按宠物归属校验当前登录用户

#### AI 助手

- 用户端问答页面
- 统一走 `/api/ai/chat`
- 文本问答固定走 DeepSeek
- 图片理解固定走 Qwen Vision
- AI 会话历史列表
- 继续历史会话
- 删除历史会话
- 图片上传识别，支持相册和拍照两种入口，结果写入同一会话并支持继续追问
- 语音录制上传，支持发送前本地预听，先转写再进入同一会话并支持继续追问
- 可为 AI 会话绑定单只宠物，后端自动带入宠物档案与近期健康/护理记录作为问答背景
- 配置 `DEEPSEEK_API_KEY` 时返回真实结果
- 配置 `QWEN_API_KEY` 时返回真实图片理解和语音转写结果
- 未配置时返回可识别的降级提示

#### 社区

- 帖子列表
- 发帖
- 点赞
- 帖子详情
- 评论列表
- 发表评论
- 我的帖子
- 编辑帖子
- 删除帖子
- 我的评论
- 删除自己的评论

#### 知识百科

- 分类列表
- 推荐文章
- 搜索文章
- 文章详情
- 点赞
- 收藏

### 4.2 管理端

#### 认证与安全边界

- 独立管理员账号体系
- 管理端只认管理员 JWT
- 普通用户 token 无法访问 `/api/admin/*`

#### 概览统计

- 总用户数
- 宠物档案数
- 疫苗记录数
- 驱虫记录数
- 体检记录数
- 护理记录数
- 帖子数
- 评论数
- 文章数
- 分类数

#### 用户管理

- 用户列表
- 关键词搜索
- 用户详情
- 删除用户

#### 内容管理

- 帖子：列表、搜索、详情、删除
- 评论：列表、搜索、详情、删除
- 分类：列表、搜索、详情、新增、编辑、删除
- 文章：列表、搜索、详情、新增、编辑、删除

### 4.3 后端约束

- 普通用户 JWT 与管理员 JWT 分离
- 社区模块统一按 JWT 中 `userId` 归属
- 护理模块按当前用户校验宠物归属
- 管理端内容管理复用现有 community / knowledge 模块，不增加第四个服务

## 5. 当前未完成范围

### 5.1 用户端未完成

- 社区预约功能前端主流程接入
- 知识百科推荐策略、后台发布工作流
- AI 快捷问题模板
- AI 语音播报

### 5.2 管理端未完成

- 系统配置
- 复杂权限管理与 RBAC
- 角色分级
- 审计日志
- 内容审核状态流转
- 趋势分析、活跃分析、留存报表

### 5.3 工程侧未完成

- 自动化测试覆盖率不足
- 三端 E2E 演示脚本未建立
- Windows 与 macOS 的启动说明虽然已补齐，但仍缺少一键健康检查脚本

## 6. 完整版推进顺序

完整版仍按“先用户核心链路，再后台扩展”的顺序推进。

### 第一阶段：稳住现有基线

目标：

- 保持三端启动、端口、文档、局域网访问方式一致
- 不再出现页面写死 `localhost`
- Web 与手机真机都能跑核心链路

验收：

- macOS 和 Windows 可按文档跑通 `backend + frontend + admin-web`
- 用户端 Web 与手机真机访问时 API 指向当前开发机后端

### 第二阶段：补用户端核心体验

目标：

- 补齐社区、知识百科已恢复模块的交互细节
- 优先补内容自管理与详情链路闭环
- 继续统一所有用户端接口调用方式

优先项：

- 知识文章浏览体验优化
- AI 问答体验优化
- AI 与宠物档案联动
- AI 常见问题快捷提问
- 社区预约流程接入

### 第三阶段：补后台运营能力

目标：

- 在现有内容管理基础上补审核与运营能力
- 强化知识内容维护闭环

优先项：

- 分类和文章发布状态
- 帖子和评论审核状态
- 举报与下架机制
- 更清晰的内容详情和筛选维度

### 第四阶段：补系统配置与数据分析

目标：

- 形成真正可运营的后台

优先项：

- AI 开关与密钥配置面板
- 推荐配置
- 内容发布开关
- 活跃、趋势、内容增长分析

### 第五阶段：补权限体系

目标：

- 从“单管理员账号”升级到可扩展权限模型

优先项：

- 管理员角色模型
- 权限点定义
- 后端 RBAC
- 操作日志

## 7. 核心接口口径

### 用户端

- 认证：`/api/users/*`
- 宠物：`/api/pets/*`
- 健康：`/api/health/*`
- 护理：`/api/care/*`
- AI：`/api/ai/*`
- 社区：`/api/community/*`
- 知识：`/api/knowledge/*`

### 管理端

- 管理员认证：`/api/admin/auth/*`
- 概览：`/api/admin/dashboard/overview`
- 用户管理：`/api/admin/users/*`
- 内容管理：
  - `/api/admin/content/posts/*`
  - `/api/admin/content/comments/*`
  - `/api/admin/content/categories/*`
  - `/api/admin/content/articles/*`

## 8. 角色与权限边界

### 普通用户

- 只能访问自己的用户端数据
- 只能访问自己宠物下的健康和护理记录
- 只能创建自己的帖子、评论、预约

### 管理员

- 只能通过管理员登录接口进入后台
- 只能用管理员 JWT 访问管理端接口
- 当前权限模型为单层管理员，不含复杂角色分级

## 9. 演示验收清单

### 用户端

1. 注册一个新用户并登录
2. 修改昵称或绑定邮箱
3. 新增宠物
4. 新增一条疫苗、一条驱虫、一条体检、一条护理
5. 编辑其中任意一条健康记录和一条护理记录
6. 进入 AI 助手发送提问
7. 上传一张图片并收到识别结果，然后继续追问
8. 录制一段语音并收到转写问答结果
9. 查看 AI 历史列表并继续一段旧会话
10. 发一篇帖子并在详情页发表评论
11. 在“我的帖子 / 我的评论”里编辑或删除自己的内容
12. 进入知识百科执行搜索并打开文章详情

### 管理端

1. 使用管理员账号登录
2. 查看概览数据
3. 搜索并查看一个用户详情
4. 查看帖子列表和评论列表
5. 新增一个知识分类
6. 新增一篇知识文章
7. 编辑分类或文章
8. 删除测试数据

### 权限验证

1. 普通用户 token 请求 `/api/admin/*` 必须失败
2. A 用户不能读取或修改 B 用户宠物下的护理记录
3. 用户端普通 token 不能作为管理员登录后台

## 10. 风险与备注

- AI 模块真实效果取决于 `DEEPSEEK_API_KEY` 与 `QWEN_API_KEY`
- 当前默认 `sql.js` 适合演示，不适合高并发或多实例部署
- 当前管理端虽然已具备内容管理，但还不是完整运营后台
- 当前社区与知识百科已恢复为正式模块，但距离“成熟产品体验”还有明显差距
